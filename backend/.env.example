# Database (MySQL). These match the defaults used in src/app.module.ts.
# When running the backend directly (npm run start:dev) you need a MySQL
# server reachable at DB_HOST:DB_PORT with this user/password and an
# (empty) database named DB_NAME — TypeORM creates the tables on startup.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=110704
DB_NAME=khmer_vegetable_market

# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USER=yourapp@gmail.com
# MAIL_PASS=your_app_password_here
# MAIL_FROM="Organic Editorial <yourapp@gmail.com>"

# CLOUDINARY_CLOUD_NAME=dyznrzmue
# CLOUDINARY_API_KEY=129381813717694
# CLOUDINARY_API_SECRET=cgYf8KQ5eDptfmsmtbrRyXmTIF0